import { Module, Scope } from '@nestjs/common';import { APP_GUARD } from '@nestjs/core';import { TenantModule } from '../tenant/tenant.module';import { JwtAuthGuard } from './jwt-auth.guard';import { PermissionsGuard } from './permissions.guard';
@Module({imports:[TenantModule],providers:[{provide:APP_GUARD,scope:Scope.REQUEST,useClass:JwtAuthGuard},{provide:APP_GUARD,scope:Scope.REQUEST,useClass:PermissionsGuard}]})export class IamModule{}
