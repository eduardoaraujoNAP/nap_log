import { drivers } from "@/lib/mock-data";

export default function MapPage() {
  return <div className="page map-page"><div className="page-heading"><div><p className="eyebrow">ACOMPANHAMENTO AO VIVO</p><h1>Mapa da operação</h1><p>Posições ilustrativas até a configuração do provedor de mapas.</p></div><span className="live-pill"><i/> Atualizações em tempo real</span></div><section className="map-layout"><aside className="map-list"><div className="map-list-head"><h2>Motoristas</h2><span>{drivers.length} visíveis</span></div><label className="search-field">⌕<input placeholder="Buscar motorista"/></label>{drivers.map(driver => <div className="map-driver" key={driver.id}><span className="avatar">{driver.initials}<i/></span><div><b>{driver.name}</b><small>{driver.vehicle}</small><span>{driver.status} · {driver.lastUpdate}</span></div></div>)}</aside><div className="map-canvas"><div className="map-grid"/><div className="map-river"/><div className="road road-a"/><div className="road road-b"/><div className="road road-c"/>{drivers.map(driver => <div className="map-pin" key={driver.id} style={{ left: `${driver.x}%`, top: `${driver.y}%` }}><span>{driver.initials}</span><small>{driver.name}</small></div>)}<div className="map-notice"><b>Prévia do mapa</b><span>Configure a chave do Mapbox para habilitar ruas, rotas e geocodificação.</span><code>NEXT_PUBLIC_MAPBOX_TOKEN</code></div><div className="map-controls"><button>＋</button><button>−</button><button>⌖</button></div></div></section></div>;
}
